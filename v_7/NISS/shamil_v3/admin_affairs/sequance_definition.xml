<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">
        <!-- Sequences for vehicle move --> 
        <record id="seq_type_vehicle_move" model="ir.sequence.type">
            <field name="name">vehicle move</field>
            <field name="code">vehicle.move</field>
        </record>
        <record id="seq_vehicle_move" model="ir.sequence">
            <field name="name">vehicle move</field>
            <field name="code">vehicle.move</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for vehicle theft --> 
        <record id="seq_type_vehicle_theft" model="ir.sequence.type">
            <field name="name">vehicle theft</field>
            <field name="code">vehicle.theft</field>
        </record>
        <record id="seq_vehicle_theft" model="ir.sequence">
            <field name="name">vehicle theft</field>
            <field name="code">vehicle.theft</field>
            <field name="padding">5</field>
        </record>

        <!-- Sequences for vehicle sale --> 
        <record id="seq_type_vehicle_sale" model="ir.sequence.type">
            <field name="name">vehicle sale</field>
            <field name="code">vehicle.sale</field>
        </record>
        <record id="seq_vehicle_sale" model="ir.sequence">
            <field name="name">vehicle sale</field>
            <field name="code">vehicle.sale</field>
            <field name="padding">5</field>
        </record>

	<!-- Sequences for vehicle code -->
        <record id="seq_type_vehicle_code" model="ir.sequence.type">
            <field name="name">vehicle code</field>
            <field name="code">fleet.vehicle.code</field>
        </record>
        <record id="seq_vehicle_code" model="ir.sequence">
            <field name="name">vehicle code</field>
            <field name="code">fleet.vehicle.code</field>
            <field name="padding">6</field>
        </record> 
    </data>
</openerp>

